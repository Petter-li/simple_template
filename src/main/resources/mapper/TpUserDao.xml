<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.emos.wx.db.dao.TbUserDao">
    <select id="haveRootUser" resultType="boolean">
        SELECT IF(COUNT(*),TRUE,FALSE) FROM tb_user WHERE root=1;
    </select>
    <insert id="insert" parameterType="HashMap">
        INSERT INTO tb_user
        SET
        open_id = #{openId},
        password = #{password},
        name = #{name},
        tel = #{tel},
        email = #{openId},
        <if test="photo!=null">
            photo = #{photo},
        </if>
        <if test="sex!=null">
            sex = #{sex},
        </if>
        <if test="hiredate!=null">
            hiredate = #{hiredate},
        </if>
        role = #{role},
        root = #{root},
        trial = #{trial},
        <if test="deptName!=null">
            dept_id = ( SELECT id FROM tb_dept WHERE dept_name = #{deptName} ),
        </if>
        status = #{status},
        create_time = #{createTime}
    </insert>
    <select id="searchIdByOpenId" parameterType="String" resultType="Integer">
        SELECT id FROM tb_user WHERE open_id=#{openId} AND status = 1
    </select>
    <select id="searchUserPermissions" parameterType="int" resultType="String">
        SELECT DISTINCT p.permission_name
        FROM tb_user u
        JOIN tb_role r on JSON_CONTAINS(u.role, CAST(r.id AS CHAR))
        JOIN tb_permission p on JSON_CONTAINS(r.permissions, CAST(p.action_id AS CHAR))
        WHERE u.id=#{userId} AND u.status = 1;
    </select>
    <select id="searchUserByOpenId" parameterType="String" resultType="TbUser">
        SELECT * FROM tb_user WHERE open_id=#{openId} AND status = 1
    </select>
    <select id="searchById" parameterType="int" resultType="TbUser">
        SELECT * FROM tb_user WHERE id=#{userId} AND status = 1
    </select>
    <update id="updateTrial" parameterType="HashMap">
        UPDATE tb_user
        SET
            trial = #{trial}
        WHERE id = #{userId} AND status = 1
    </update>
    <update id="updateInfo" parameterType="HashMap">
        UPDATE tb_user
        SET
            name = #{name},
            photo = #{photo}
        WHERE id = #{userId} AND status = 1
    </update>
</mapper>