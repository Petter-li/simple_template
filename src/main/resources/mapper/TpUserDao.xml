<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.simple_template.db.dao.TpUserDao">
    <insert id="insert" parameterType="HashMap" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tp_user
        SET
        name = #{name},
        tel = #{tel},
        <if test="openId!=null">
            open_id = #{openId},
        </if>
        <if test="password!=null">
            password = #{password},
        </if>
        <if test="photo!=null">
            photo = #{photo},
        </if>
        <if test="sex!=null">
            sex = #{sex},
        </if>
        role = #{role},
        root = #{root},
        status = #{status}
    </insert>
    <select id="select" parameterType="Long" resultType="TpUser">
        SELECT * FROM tp_user WHERE id = #{id} AND status = 1
    </select>
    <select id="selectByTel" parameterType="String" resultType="TpUser">
        SELECT * FROM tp_user WHERE tel = #{tel} AND status = 1
    </select>
    <select id="searchUserPermissions" parameterType="Long" resultType="String">
        SELECT DISTINCT p.permission_name
        FROM tp_user u
                 JOIN tp_role r on JSON_CONTAINS(u.role, CAST(r.id AS CHAR))
                 JOIN tp_permission p on JSON_CONTAINS(r.permissions, CAST(p.action_id AS CHAR))
        WHERE u.id=#{userId} AND u.status = 1;
    </select>
</mapper>